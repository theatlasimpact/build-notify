{
  "name": "build-notify",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'no build step required'"
  },
  "devDependencies": {
    "wrangler": "3.80.0"
  }
}
